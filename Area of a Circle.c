#include <stdio.h>
   int main () {
      float n = 3.14159;
      float raio =0;
      float area;
      scanf ("%f", &raio );
      area = n * (raio*2);
      printf ("A= %.4f \n",area);

       return 0;
   }
